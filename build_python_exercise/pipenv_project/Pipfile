[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
requests = ">=2.28.0"
click = ">=8.0.0"
pydantic = ">=1.10.0"
rich = {version = ">=12.0.0", optional = true}

[dev-packages]
pytest = ">=7.0.0"
pytest-cov = ">=4.0.0"
black = ">=22.0.0"
flake8 = ">=5.0.0"
mypy = ">=1.0.0"
pre-commit = ">=2.20.0"
ipython = "*"
jupyter = "*"

[requires]
python_version = "3.8"

[scripts]
test = "pytest tests/"
test-cov = "pytest --cov=pipenv_demo --cov-report=html tests/"
lint = "flake8 src/ tests/"
format = "black src/ tests/"
type-check = "mypy src/"
dev = "python -m pipenv_demo.cli"